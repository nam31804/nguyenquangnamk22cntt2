@model IEnumerable<NguyenQuangNamK22CNTT2.Models.giohang>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.soluong)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.khachhang.khachhang_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.sanpham.tieude)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.soluong)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.khachhang.khachhang_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.sanpham.tieude)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.giohang_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.giohang_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.giohang_id })
        </td>
    </tr>
}

</table>
